/* Diskio.plp.ins, Dutil, JEW, 20-Mar-81
   Diskio routine declaration.  */
   %nolist;

dcl
   diskio entry (bit(16), bin(31), bin, 1, 2, 3 bin(31), 3 bin(31), 3 bin,
                3 bin, 3 bin(31), 3 bin(31), 3 bin, 3 (5) bin, 2 (0:1023) bin,
                bin);

%list;

/* end diskio.plp.ins */


/* disk_record.plp.ins, jew, 1-25-81
   Insert file to define disk record template. */
   %nolist;

dcl
   1  disk_record  based,
      2  rec_header,
         3  (cra, bra)         bin(31),
         3  (count, type)      bin,
         3  (next, back)       bin(31),
         3  level              bin,
         3  reserved(5)        bin,
      2  data (0:1023)         bin;
%list;

/* end disk_record.plp.ins */


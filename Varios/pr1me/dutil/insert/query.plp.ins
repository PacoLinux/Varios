/* Query.plp.ins, Dutil, JEW, 20-Mar-81
   Defines QUERY routine and keys.  */
   %nolist;

dcl
   query entry (char(*) var, bin) returns (bit);

%replace
   def_no   by 1,
   def_yes  by 2,
   no_def   by 3;

%list;

/* end query.plp.ins */


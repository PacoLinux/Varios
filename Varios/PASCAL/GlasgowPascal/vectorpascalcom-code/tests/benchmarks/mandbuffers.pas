unit mandbuffers;
interface uses mandspec;
type
	 
	comparray = array[  edge,edge] of real;
	pcomparray = ^ comparray;
        
var   xx,y,x,cx,cy:comparray;
      times:array[edge,edge] of integer;
implementation
begin
{ new(xx);new(y);new(x);new(cx);{}
 
end.

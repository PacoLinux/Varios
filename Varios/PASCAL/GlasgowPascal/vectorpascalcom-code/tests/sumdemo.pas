Program proddemo;
Var a:array[1..5] of Integer; Y:integer;
Begin
	{ unicode version}
	a ← ⍳ 0; { form integers from 1 to 5 }
	Writeln(a);
	Y ←∏ a; { get their product }
	Writeln( y, √y);
	{ now using ascii }
            a:= iota 0;
	writeln(a);
	y:= \ * a;
	writeln (y, sqrt(y));
End.



	


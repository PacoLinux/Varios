PROGRAM grade;
VAR marks,grades:ARRAY[1..8] OF integer;
const one=1;
      two=2;
      three=3;
BEGIN
     
     READ(marks);
     grades:=IF marks>69 THEN one
           {  ELSE IF marks>=g50 THEN 2}
                  ELSE three;
     WRITE(GRADES:3);
END.
 
